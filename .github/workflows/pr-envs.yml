name: PR Test
on:
  pull_request:

jobs:
  build:
    uses: PADAS/serca-pipelines/.github/workflows/pipeline-kubernetes-node-main.yml@main
    secrets: inherit
    with:
      app-name: das-web-react
      repository: europe-west3-docker.pkg.dev/padas-app/er-mt/das-web-react
