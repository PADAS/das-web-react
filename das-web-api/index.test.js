test('defining an express app and creates an http server with it', () => {

});

test('invoking middleware config', () => {

});

test('invoking route config', () => {

});

test('listening on the environment-configured port', () => {

});