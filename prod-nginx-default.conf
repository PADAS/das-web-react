server {
    listen       80;
    listen  [::]:80;
    location = /beta {
        rewrite ^(.*)$ $1/ last;
    }
    location /beta/ {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }
}
