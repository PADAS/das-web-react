describe('duration humanizers', () => {
  it('has a configuration for full abbreviated format', () => {

  });

  it('has a configuration for minutes only', () => {

  });

  it('has a configuration for abbreviated formats with fidelity down to the minute', () => {

  });

  it('has a configuration for full-language display', () => {

  });
});